<% console.log('info', version) %>
<fieldset>
    <legend>Version info</legend>
    <div class="b-version-info">
        <label for=""></label>
        <dl>
            <dt>Title</dt>
            <dd>
                <input type="text" value="<%=version.getTitle()%>"/>
            </dd>
            <dt>
            Parent
            </dt>
            <dd>
                <% if (version.hasParent()) { %>
                <a href="#"><%version.getParent().getTitle()%></a>
                <% } else { %>
                Root
                <% } %>
            </dd>
            <dt>Created at</dt>
            <dd><%=version.getCreatedAt().format('DD.MM.YYYY hh:mm:ss')%></dd>
            <dt>Published</dt>
            <dd>
                <% if (version.isPublished()) { %>
                Published at<%=version.getPublishedAt().format('DD.MM.YYYY hh:mm:ss')%>
                <% } else { %>
                Unpublished 
                <% } %>
            </dd>
        </dl>
    </div>
</fieldset>